## Open source python password manager

### Installation

1. You need to have [python](https://python.org) installed
2. Run this command in your terminal `pip install ospm`

### Usage
To start using the password manager, after installing it, run `ospm` in your terminal. Or run `python -m ospm` if the previous command doesn't work